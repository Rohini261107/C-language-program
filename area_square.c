#include<stdio.h>
int main()
{
    int side,area,i;

    for(i=1;i<=1;i++)
    {
        printf("Enter the sides of the square:");
        scanf("%d",&side);

        area=side*side;

        printf("area of square is:%d \n");
    }

    return 0;
}